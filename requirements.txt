# Core dependencies
python-dotenv==1.0.0

# OpenAI SDK (for Whisper STT and TTS)
openai==1.59.5

# Groq SDK (for fast LLM inference)
groq==0.13.1

# ElevenLabs SDK (optional, for premium TTS)
elevenlabs==2.27.0

# Audio processing
pydub==0.25.1
numpy>=1.24.0

# HTTP client for TTS streaming
httpx>=0.27.0

# WebSocket server for real-time streaming
websockets>=13.1

# Utility
requests==2.32.3

# NOTE: sentence-transformers and chromadb removed for Render free tier
# Using lightweight keyword-based retrieval instead (rag_retriever.py)
# If you have >1GB RAM, uncomment these for better retrieval:
# chromadb>=0.4.22
# sentence-transformers>=2.3.1
